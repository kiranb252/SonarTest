pipeline {
    agent any
     stages {
           stage ('Static Analysis'){
              steps{
                withSonarQubeEnv('SonarTestServer') {
                  sh "mvn clean install sonar:sonar"
				}
			}
		}
                      
	}
}
