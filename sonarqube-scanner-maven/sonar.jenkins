pipeline {
    agent any
     stages {
           stage ('Static Analysis'){
              steps{
                withSonarQubeEnv('Bluesky Sonar Server') {
                  sh "mvn sonar:sonar -DBUILD_NUMBER=${env.BUILD_NUMBER}"
				}
			}
		}
                      
	}
}
